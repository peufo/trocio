version: '3.8'
services:
  mongo:
    image: mongo
    restart: always

  trocio:
    container_name: trocio
    image: node:18-alpine
    depends_on:
      - mongo
    command: sh -c "npm i && npm run dev"
    ports:
      - 5001:5000
    working_dir: /app
    volumes:
      - ./:/app
